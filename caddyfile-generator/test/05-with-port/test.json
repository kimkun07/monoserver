{
  "description": "Should generate Caddyfile with subdomain routing when x-caddy-port is provided",
  "shouldFail": false,
  "params": {
    "composePath": "./compose.yaml",
    "outputDir": "."
  }
}
