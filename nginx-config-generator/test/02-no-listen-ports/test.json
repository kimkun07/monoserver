{
  "description": "Should use default listen port [80] when x-monoserver-listen-ports is not specified",
  "shouldFail": false,
  "params": {
    "composePath": "./compose.yaml",
    "outputDir": "./nginx/conf.d"
  }
}
