{
  "description": "Should use $server_port when x-monoserver-default-port is not specified",
  "shouldFail": false,
  "params": {
    "composePath": "./compose.yaml",
    "outputDir": "./nginx"
  }
}
