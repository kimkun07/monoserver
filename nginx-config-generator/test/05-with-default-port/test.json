{
  "description": "Should use specified port when x-monoserver-default-port is provided",
  "shouldFail": false,
  "params": {
    "composePath": "./compose.yaml",
    "outputDir": "./nginx/conf.d",
    "nginxService": "monoserver-nginx-main"
  }
}
