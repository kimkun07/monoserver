# This file is auto-generated by nginx-config-generator
# DO NOT EDIT MANUALLY - changes will be overwritten
# Service-specific location blocks for path-based routing

  # Route for service: hello
  location /hello/ {
    # Using a variable forces nginx to resolve DNS at request time
    # This prevents startup failures when upstream containers aren't ready yet
    set $upstream_hello hello;
    # Strip the /hello/ prefix from the request
    rewrite ^/hello/(.*)$ /$1 break;
    proxy_pass http://$upstream_hello:5678;
  }

  # Route for service: whoami
  location /whoami/ {
    # Using a variable forces nginx to resolve DNS at request time
    # This prevents startup failures when upstream containers aren't ready yet
    set $upstream_whoami whoami;
    # Strip the /whoami/ prefix from the request
    rewrite ^/whoami/(.*)$ /$1 break;
    proxy_pass http://$upstream_whoami:80;
  }

  # Route for service: happy-server
  location /happy-server/ {
    # Using a variable forces nginx to resolve DNS at request time
    # This prevents startup failures when upstream containers aren't ready yet
    set $upstream_happy_server happy-server;
    # Strip the /happy-server/ prefix from the request
    rewrite ^/happy-server/(.*)$ /$1 break;
    proxy_pass http://$upstream_happy_server:3000;
  }
